import React,{Component} from 'react';
import Register from '../../components/Register/Inf'

export default class RegisterPage extends Component{
    render(){
        return(
            <div id="Register">
                <Register/>
            </div>
        )
    }
}